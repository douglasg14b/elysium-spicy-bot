version: '3.8'
services:
    discord-spicy-bot:
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
            - sqlite-data:/app/db
        environment:
            - NODE_ENV=production
        restart: unless-stopped

volumes:
    sqlite-data:
        driver: local
