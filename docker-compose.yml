version: '3.8'
services:
    discord-spicy-bot:
        build: .
        volumes:
            - sqlite-data:/app/db
        environment:
            - NODE_ENV=production
        restart: unless-stopped
        # Add other necessary configuration such as ports if needed

volumes:
    sqlite-data:
        driver: local
