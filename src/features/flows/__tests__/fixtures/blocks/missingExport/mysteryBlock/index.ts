/**
 * A block directory whose entry module exports everything except `block`. The
 * failure mode that would otherwise be a block silently absent from the palette.
 */
export const almostTheRightName = { type: 'fixture.mystery' };
